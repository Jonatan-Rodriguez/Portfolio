export interface Translations {
  nav: {
    home: string
    about: string
    formacion: string
    proyectos: string
    experiencia: string
    contacto: string
  }
  connectButton: {
    cta: string
  }
  hero: {
    developerWords: [string, string]
    ariaRole: string
    role: string
    study: string
    greeting: string
  }
}