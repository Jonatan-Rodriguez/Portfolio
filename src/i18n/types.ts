export interface Translations {
  nav: {
    home: string
    about: string
    formacion: string
    proyectos: string
    experiencia: string
    contacto: string
  }
  connectButton: {
    cta: string
  }
}